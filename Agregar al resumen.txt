db.create/db.update con relaciones muchos a muchos

softDelete (paranoid en los modelos)

Scope en los modelos

Template literals (Template strings)

Spread operator (ver lineas 100 a 111 del archivo usersController.js de mi_cafe)

Hacer una carpeta por cada componente (con el nombre del componente) y dentro de la carpeta tener un index.js (ya que despues cuando lo importemos donde lo necesitemos no hace falta aclarar que es el archivo index.js) que es el que exportamos al final